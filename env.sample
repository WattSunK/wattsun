# WattSun sample environment file (do not commit real secrets)

# Admin bootstrap (used only on first run if missing)
ADMIN_BOOTSTRAP_EMAIL=admin@example.com
ADMIN_BOOTSTRAP_PASSWORD=ChangeMe123!

# SQLite DB (dev sample). Override per environment.
DB_PATH_USERS=./data/dev/wattsun.dev.db
DB_PATH_ADMIN=./data/dev/wattsun.dev.db
SQLITE_DB=./data/dev/wattsun.dev.db

# SMTP (notifications worker)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_gmail@example.com
SMTP_PASS=your_app_password_here
SMTP_FROM="WattSun <your_gmail@example.com>"

# Session secret and server port
SESSION_SECRET=change_me
PORT=3001
NODE_ENV=development

# Optional: worker tuning
# WORKER_BATCH=20
# WORKER_INTERVAL_MS=60000

# For QA/Prod, keep real values in a private file referenced by ENV_FILE
# and never commit the file with credentials.
