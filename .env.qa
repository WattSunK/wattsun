# App/server
PORT=3000
NODE_ENV=qa
SESSION_SECRET=qa-local-secret

# Admin bootstrap (only if QA seeds users on first run)
ADMIN_BOOTSTRAP_EMAIL=skamunyu@gmail.com
ADMIN_BOOTSTRAP_PASSWORD=Pass123

# SQLite paths (separate from dev)
SQLITE_MAIN=/volume1/web/wattsun/data/qa/wattsun.qa.db
SQLITE_DB=${SQLITE_MAIN}
DB_PATH_USERS=${SQLITE_MAIN}
DB_PATH_INVENTORY=./data/qa/inventory.qa.db
DB_PATH_ADMIN=${SQLITE_MAIN}
# Additional path knobs
# DB_PATH_OVERLAY=./data/qa/overlay.qa.db
# WATTSUN_DB_PATH=${SQLITE_MAIN}
# WS_ADMIN_DB=${SQLITE_MAIN}
# DB_PATH=./data/qa/marketplace.qa.db

# SMTP (use a QA mailbox or sandbox like Mailtrap)
# Example Mailtrap values shown; replace with your creds
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=mainakamunyu@gmail.com
SMTP_PASS=wepodlsxxwavsmof
SMTP_FROM="WattSun QA <mainakamunyu@gmail.com>"

# Notifications worker (dry-run by default in QA)
WORKER_BATCH=20
WORKER_INTERVAL_MS=60000
NOTIFY_DRY_RUN=0

# Alerts/watcher
EMAIL_FROM="WattSun QA <no-reply@wattsun.co.ke>"
ALERT_EMAIL_TO=alerts-qa@wattsun.co.ke
ALERT_POLL_MS=30000

# PID/audit alerts
PID_ALERT_LOG=./logs/alerts-qa.log
AUDIT_POLL_MS=60000

# Health/log dirs
LOG_DIR=./logs
RUN_DIR=./run

# Compatibility with your scripts
LOG_FILE=./logs/app.out

# Misc tooling
# ROOT=.
# ENV_FILE=./env


